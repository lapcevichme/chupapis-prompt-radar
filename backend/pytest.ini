[pytest]
addopts = -ra --strict-markers
asyncio_mode = auto
testpaths = tests
markers =
    unit: unit tests
    integration: tests with app and postgres adapters
    contract: contract tests against docs/contracts schemas
